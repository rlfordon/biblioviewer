# dependencies
node_modules/

# bibliography content stays private — built viewers and data (with cached
# article snapshots) are shared directly with readers, never published here
bibliography.json
*.html
!template.html
# ...except the throwaway test fixture, which cites only public pages and
# carries no snapshots
!test-fallback/bibliography.json

# packaged skill snapshots (build artifacts; distributed via GitHub Releases)
*.zip

.DS_Store
